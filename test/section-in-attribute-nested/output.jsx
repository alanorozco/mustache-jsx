<a
  class={section(view("classes"), (_0_classes, _i_0) => (
    <>{section(view("value", _0_classes), (_1_value, _i_1) => _1_value)}$</>
  ))}
>
  TODO: The output for this test is very wrong.
  <br />
  class contains fragments, when it should be strings all the way down.
</a>;
